[package]
name = "loopr"
version.workspace = true
edition.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true

[[bin]]
name = "loopr"
path = "src/main.rs"

[lib]
name = "loopr"
path = "src/lib.rs"

[dependencies]
# Internal workspace crates (path deps; the full dependency graph per docs/vision.md)
derive = { path = "../derive" }
telemetry = { path = "../telemetry" }
store = { path = "../store" }
domain = { path = "../domain" }
llm = { path = "../llm" }
tools = { path = "../tools" }
worktree = { path = "../worktree" }
ipc = { path = "../ipc" }
context = { path = "../context" }
decomposer = { path = "../decomposer" }
agents = { path = "../agents" }
integrator = { path = "../integrator" }

# Workspace-shared external deps
chrono = { workspace = true }
clap = { workspace = true }
eyre = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml = { workspace = true }
tracing = { workspace = true }
tracing-appender = { workspace = true }
tracing-subscriber = { workspace = true }
thiserror = "2.0.18"

[dev-dependencies]
tempfile = "3.27.0"
