name: hierarchy
description: Shared lifecycle for Plan, Spec, and Phase records

states:
  - draft
  - pending
  - active
  - complete
  - superseded
  - abandoned

terminal:
  - complete
  - superseded
  - abandoned

transitions:
  draft:
    pending: { by: [coordinator] }
    active: { by: [coordinator] }
    superseded: { by: [coordinator] }
    abandoned: { by: [coordinator] }
  pending:
    active: { by: [coordinator] }
    superseded: { by: [coordinator] }
    abandoned: { by: [coordinator] }
  active:
    complete: { by: [coordinator, decomposer] }
    superseded: { by: [coordinator] }
    abandoned: { by: [coordinator] }

overrides: {}

guards: {}
