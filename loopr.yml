name: "loopr"
debug: true

project:
  repo_path: /home/saidler/repos/scottidler/todo-app
  worktree_dir: .worktrees

validator:
  enabled: false

integrator:
  enabled: true
  interval_secs: 10

agents:
  enabled: true
