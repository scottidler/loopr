// Common test utilities for FSM tests.
// The exhaustive transition matrix tests are now in src/fsm/tests.rs.
// This module is retained for any shared helpers needed by remaining tests
// (dispatch, lock, status).
